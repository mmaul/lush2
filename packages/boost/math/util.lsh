(defmacro defnmp_accessor (cls fn ctype)
  `(defmethod ,cls ,fn (z)
  (declare (-double-) z)
  (let ((r 0)
	(s :this:dist))
    (declare (-double-) r)
    (declare (-gptr- ,ctype) s)
    #{ $r = quantile(*$s,$z); #}
    r))
)
